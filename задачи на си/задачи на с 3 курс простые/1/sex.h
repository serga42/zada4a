const float KM_TO_MILES = 0.62137;
    const float MILES_TO_KM = 1.60934;
    float a;
    const float TRE = 0.5;	const float KM_TO_MILES = 0.62137;
    const float MILES_TO_KM = 1.60934;
    float a;
    const float TRE = 0.5;
    float h;//переменая a
    int num;
    float b;//переменная a
    int n;
    int a2;
    int b2;
    STARTUPINFO si;
    PROCESS_INFORMATION pi;
    ZeroMemory( &si, sizeof(si) );
    si.cb = sizeof(si);
    ZeroMemory( &pi, sizeof(pi) );
    float h;//переменая a
    int num;
    float b;//переменная a
    int n;
    int a2;
    int b2;
    char variant;